extends layout

block content
  if auto
    meta(http-equiv="refresh" content=time)
  h1= title
  h5 Players online: #{playercount}
  table(class='table')
    thead
      tr
        th Name
        th Time Online (Hours:Minutes)
    tbody
      each player in players
        tr
          td= player.name
          td= player.time
